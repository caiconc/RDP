name: RDP Win32-Virtual64-Remote64

on:
  workflow_dispatch:

jobs:
  high-perf-rdp:
    runs-on: windows-latest
    timeout-minutes: 360
    
    steps:
      # 1. Cấu hình cơ bản & Firewall
      - name: Configure Core RDP & Firewall
        run: |
          Set-ItemProperty -Path 'HKLM:\System\CurrentControlSet\Control\Terminal Server' -Name "fDenyTSConnections" -Value 0 -Force
          Set-ItemProperty -Path 'HKLM:\System\CurrentControlSet\Control\Terminal Server\WinStations\RDP-Tcp' -Name "UserAuthentication" -Value 0 -Force
          Set-ItemProperty -Path 'HKLM:\System\CurrentControlSet\Control\Terminal Server\WinStations\RDP-Tcp' -Name "SecurityLayer" -Value 0 -Force
          
          # Firewall: Chỉ cho phép IP Tailscale
          netsh advfirewall firewall delete rule name="RDP-Tailscale"
          netsh advfirewall firewall add rule name="RDP-Tailscale" dir=in action=allow protocol=TCP localport=3389 remoteip=100.64.0.0/10 profile=any
          
          Restart-Service -Name TermService -Force

      # 2. Cài đặt Tailscale
      - name: Install Tailscale
        run: |
          $tsUrl = "https://pkgs.tailscale.com/stable/tailscale-setup-latest.msi"
          $installerPath = "$env:TEMP\tailscale.msi"
          Invoke-WebRequest -Uri $tsUrl -OutFile $installerPath
          Start-Process msiexec.exe -ArgumentList "/i", "`"$installerPath`"", "/quiet", "/norestart" -Wait
          Remove-Item $installerPath -Force

      - name: Connect Tailscale
        run: |
          & "$env:ProgramFiles\Tailscale\tailscale.exe" up --authkey=${{ secrets.TAILSCALE_AUTH_KEY }} --hostname=gh-runner-$env:GITHUB_RUN_ID --accept-routes
          $tsIP = $null
          $retries = 0
          while (-not $tsIP -and $retries -lt 20) {
              $tsIP = (& "$env:ProgramFiles\Tailscale\tailscale.exe" ip -4) -join ''
              Start-Sleep -Seconds 5
              $retries++
          }
          if (-not $tsIP) { Write-Error "Tailscale IP error"; exit 1 }
          echo "TAILSCALE_IP=$tsIP" | Out-File -FilePath $env:GITHUB_ENV -Encoding utf8 -Append

      # 3. Tạo User RDP
      - name: Create User
        run: |
          $password = -join ((65..90) + (97..122) + (48..57) | Get-Random -Count 16 | % {[char]$_})
          $securePass = ConvertTo-SecureString $password -AsPlainText -Force
          if (-not (Get-LocalUser -Name "RDP" -ErrorAction SilentlyContinue)) {
            New-LocalUser -Name "RDP" -Password $securePass -AccountNeverExpires
          } else { Set-LocalUser -Name "RDP" -Password $securePass }
          Add-LocalGroupMember -Group "Remote Desktop Users" -Member "RDP"
          Add-LocalGroupMember -Group "Administrators" -Member "RDP"
          echo "RDP_PASSWORD=$password" | Out-File -FilePath $env:GITHUB_ENV -Encoding utf8 -Append

      # --- [YÊU CẦU 1] WIN32 PRIORITY ---
      - name: Set Win32Priority (CPU Optimization)
        run: |
          # Giá trị 38 (Decimal) = 0x26 (Hex)
          # Ý nghĩa: Ưu tiên ứng dụng Foreground (Programs), Short intervals, Variable quantum.
          # Giúp giảm độ trễ khi di chuột và gõ phím.
          reg add "HKLM\SYSTEM\CurrentControlSet\Control\PriorityControl" /v Win32PrioritySeparation /t REG_DWORD /d 38 /f
          Write-Host "Win32PrioritySeparation set to 38 (Programs Priority)"

      # --- [YÊU CẦU 2] VIRTUAL64 (DRIVER MÀN HÌNH ẢO) ---
      - name: Install Virtual64 (usbmmidd Display Driver)
        run: |
          # Tải driver giả lập màn hình 64-bit
          $url = "https://www.amyuni.com/downloads/usbmmidd_v2.zip"
          $dest = "C:\usbmmidd.zip"
          $extract = "C:\usbmmidd"
          Invoke-WebRequest -Uri $url -OutFile $dest
          Expand-Archive -Path $dest -DestinationPath $extract -Force
          
          # Cài đặt Driver 64-bit
          cd $extract
          Write-Host "Installing Virtual Display Driver..."
          & .\deviceinstaller64.exe install usbmmidd.inf usbmmidd
          
          # Kích hoạt màn hình ảo
          Write-Host "Enabling Virtual Display..."
          & .\deviceinstaller64.exe enableidd 1
          
          Write-Host "Virtual64 Display Active."

      # --- [YÊU CẦU 3] REMOTE64 (RDP HARDWARE ACCELERATION) ---
      - name: Optimize Remote64 (RemoteFX & GPU)
        run: |
          $rdpPath = "HKLM\SYSTEM\CurrentControlSet\Control\Terminal Server\WinStations\RDP-Tcp"
          
          # Bật RemoteFX (Giao thức đồ họa cao cấp)
          reg add $rdpPath /v EnableRemoteFX /t REG_DWORD /d 1 /f
          
          # Ép buộc Hardware Acceleration (Kể cả khi dùng Virtual GPU)
          reg add $rdpPath /v EnableHardwareAcceleration /t REG_DWORD /d 1 /f
          
          # Tối ưu hóa băng thông cho Video (Bỏ Audio)
          reg add $rdpPath /v fEnableVideoRedirection /t REG_DWORD /d 1 /f
          reg add $rdpPath /v VideoCompression /t REG_DWORD /d 1 /f
          reg add $rdpPath /v fEnableAudioRedirection /t REG_DWORD /d 0 /f
          
          # Frame Rate ổn định cho Virtual GPU
          reg add $rdpPath /v DwmFrameRate /t REG_DWORD /d 60 /f
          
          # Tắt hiệu ứng thừa (Visuals)
          $visualPath = "HKCU\Software\Microsoft\Terminal Server Client"
          reg add $visualPath /v DisableWallpaper /t REG_DWORD /d 1 /f
          reg add $visualPath /v DisableThemes /t REG_DWORD /d 1 /f

      - name: System Cleanup (Disk & Services)
        run: |
          # Tắt Defender Real-time (Tăng tốc I/O cực mạnh)
          Set-MpPreference -DisableRealtimeMonitoring $true -Force
          
          # Tắt các service không cần thiết
          $services = @("SysMain", "DiagTrack", "WSearch", "Audiosrv")
          foreach ($s in $services) {
            Stop-Service -Name $s -Force -ErrorAction SilentlyContinue
            Set-Service -Name $s -StartupType Disabled
          }
          powercfg -setactive SCHEME_MIN

      - name: Verify & Keep Alive
        run: |
          Write-Host "=== RDP INFO ==="
          Write-Host "IP: $env:TAILSCALE_IP"
          Write-Host "User: RDP"
          Write-Host "Pass: $env:RDP_PASSWORD"
          Write-Host "Config: Win32Priority(38) | Virtual64(Active) | Remote64(Accel)"
          Write-Host "================"
          while ($true) { Start-Sleep -Seconds 180; Test-Connection 8.8.8.8 -Count 1 -Quiet | Out-Null }
